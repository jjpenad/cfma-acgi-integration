from flask import render_template
from utils import setup_logging

logger = setup_logging()

def init_page_routes(app):
    """Initialize page routes"""
    # Routes moved to main.py for better organization and authentication
    pass
